import { AlertTriangle, KeyRound, RefreshCw } from "lucide-react"
import { Button } from "@/components/ui/button"
import { Card, CardContent } from "@/components/ui/card"
import { Skeleton } from "@/components/ui/skeleton"

export function VoucherSkeleton() { return <div className="space-y-6 p-4 sm:p-6 lg:p-8" aria-label="Loading vouchers"><div className="space-y-2"><Skeleton className="h-8 w-40" /><Skeleton className="h-4 w-full max-w-xl" /></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{["a", "b", "c", "d"].map((id) => <Skeleton key={id} className="h-32" />)}</div><Skeleton className="h-12 w-full" /><Skeleton className="h-96 w-full" /></div> }
export function VoucherEmptyState({ filtered, onReset, onGenerate }: { readonly filtered: boolean; readonly onReset: () => void; readonly onGenerate: () => void }) { return <Card><CardContent className="flex min-h-56 flex-col items-center justify-center text-center"><KeyRound className="size-8 text-muted-foreground" aria-hidden="true" /><h2 className="mt-4 text-lg font-semibold text-foreground">{filtered ? "No vouchers found" : "Generate your first voucher"}</h2><p className="mt-1 max-w-md text-sm text-muted-foreground">{filtered ? "Try adjusting your search or filters." : "Create voucher codes that customers can use instead of QRIS at your photo booth kiosks."}</p><Button className="mt-4" onClick={filtered ? onReset : onGenerate}>{filtered ? "Reset Filters" : "Generate Voucher"}</Button></CardContent></Card> }
export function VoucherErrorState({ onRetry }: { readonly onRetry: () => void }) { return <Card><CardContent className="flex min-h-56 flex-col items-center justify-center text-center"><AlertTriangle className="size-8 text-destructive" aria-hidden="true" /><h2 className="mt-4 text-lg font-semibold text-foreground">Unable to load vouchers</h2><p className="mt-1 text-sm text-muted-foreground">Try loading the local voucher data again.</p><Button className="mt-4" variant="outline" onClick={onRetry}><RefreshCw aria-hidden="true" /> Retry</Button></CardContent></Card> }
